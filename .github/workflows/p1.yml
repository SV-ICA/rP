# 2024-12-29

name: Pipeline p1.yml

on:
  workflow_call:

jobs:
  j1:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/a1
      - uses: ./.github/actions/b1
